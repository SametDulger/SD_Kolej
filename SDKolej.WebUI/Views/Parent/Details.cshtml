@model SDKolej.Business.DTOs.ParentDto
@{
    ViewData["Title"] = "Veli Detay";
}
<h2>Veli Detay</h2>
<div class="card">
    <div class="card-body">
        <h5 class="card-title">@Model.FirstName @Model.LastName</h5>
        <a asp-action="Index" class="btn btn-secondary">Geri Dön</a>
    </div>
</div> 