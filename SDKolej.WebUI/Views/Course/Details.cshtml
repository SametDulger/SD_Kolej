@model SDKolej.Business.DTOs.CourseDto
@{
    ViewData["Title"] = "Ders Detay";
}
<h2>Ders Detay</h2>
<div class="card">
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
        <p class="card-text">Üst Ders: @Model.ParentCourseId</p>
        <a asp-action="Index" class="btn btn-secondary">Geri Dön</a>
    </div>
</div> 